# Grafana MCP Server
GRAFANA_URL=https://grafana.example.com
GRAFANA_TOKEN=glsa_xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# VictoriaLogs MCP Server (via VMAuth)
VL_INSTANCE_ENTRYPOINT=https://victorialogs.pud.ink
VL_INSTANCE_BEARER_TOKEN=your-bearer-token-here
VL_DEFAULT_TENANT_ID=0:0

# VMAuth Credentials (for reference, stored in values.secret.yaml)
# UI Basic Auth: admin / <password>
# MCP Bearer Token: <token>
